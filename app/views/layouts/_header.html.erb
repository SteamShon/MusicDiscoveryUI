<header class="navbar navbar-fixed-top navbar-inverse">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "Music Discovery", signed_in?  ? tags_path : root_path , id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><%= link_to "Home", signed_in? ? tags_path : root_path %></li>
          <li><%= link_to "Artist", signed_in? ? artist_tags_path : root_path %></li>
          <li><%= link_to "Song", signed_in? ? tags_path : root_path %></li>
          <li><%= link_to "Help", '#' %></li>
          <% if signed_in? %>
            <li><%= link_to "Users", users_path %></li>
            <li><%= link_to "Sign out", signout_path, method: "delete" %></li>
          <% else %>
            <li><%= link_to "Sign in", signin_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>
